name: Quick stock count display
on:
  push:
    paths:
      - '.github/quick-stock-count.trigger'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Patch bot
        run: python .github/quick-stock-count.py
      - name: Syntax check
        run: python -m py_compile bot.py
      - name: Commit and clean temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/quick-stock-count.py .github/quick-stock-count.trigger .github/workflows/quick-stock-count.yml
          git add bot.py .github
          git commit -m "Show live quick stock counts and simplify admin menu"
          git push origin main
