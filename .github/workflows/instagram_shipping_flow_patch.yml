name: Instagram shipping flow patch

on:
  push:
    paths:
      - 'instagram_shipping_flow_trigger.txt'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply Instagram shipping flow patch
        run: python3 tools/instagram_shipping_flow_patch.py
      - name: Validate Python
        run: python3 -m py_compile bot.py cloud_bridge.py
      - name: Commit source update
        run: |
          git config user.name "Mokhirbek9901"
          git config user.email "ismoilovmokhirbek@gmail.com"
          git add bot.py cloud_bridge.py
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Add optional Instagram shipping address capture"
          git push origin main
